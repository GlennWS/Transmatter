// Speed of the player (5 pixels per frame)
/*playerVel = 5;

move_up = keyboard_check(ord('W')
move_left = keyboard_check(ord('A')
move_down = keyboard_check(ord('S')
move_right = keyboard_check(ord('D')

if (move_up && y > room_height / 2)
{
	y -= playerVel;
}

if (move_left && x > sprite_width / 2)
{
	x -= playerVel;
}

if (move_down && y < room_height - sprite_width / 2)
{
	y += playerVel;
}

if (move_right && x < room_height - sprite_width / 2)
{
	x += playerVel;
}*/

// A few of these lines may have to be separate scripts.